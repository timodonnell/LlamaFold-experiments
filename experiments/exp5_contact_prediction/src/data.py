"""Dataset and vocabulary for protein contact prediction (random-3-bins).

Documents come pre-formatted from local parquet files generated by the contactdoc
pipeline using the random-3-bins scheme. Each document contains an amino acid
sequence and distance-binned atomic contacts with correction tokens, false contact
injection, and pLDDT bin tokens.

Document format:
    <random-3-bins>
    <begin_sequence>
    <MET> <GLY> <VAL> ...
    <begin_contacts>
    <non-correction> <p15> <p66> <CG2> <CE> <bin_lt4>
    <correction> <p15> <p58> <CD1> <CB> <bin_lt4>
    <plddt_80_85>
    ...
    <end_contacts>
    <end>

Each contact consists of 6 tokens: correction marker, two position tokens,
two atom tokens, and a distance bin token.
Positions are 1-indexed (p1 = first residue in the sequence).
Contacts are in random order. A pLDDT bin token appears once per document.
"""

from __future__ import annotations

from collections import defaultdict

import numpy as np

# Control / structural tokens
CONTROL_TOKENS = [
    "<random-3-bins>",
    "<begin_sequence>",
    "<begin_contacts>",
    "<end_contacts>",
    "<end>",
]

# Correction markers
CORRECTION_TOKENS = [
    "<correction>",
    "<non-correction>",
]

# Distance bin tokens
DISTANCE_BIN_TOKENS = [
    "<bin_lt4>",
    "<bin_4_12>",
    "<bin_gt12>",
]

# pLDDT bin tokens
PLDDT_TOKENS = [
    "<plddt_lt70>",
    "<plddt_70_75>",
    "<plddt_75_80>",
    "<plddt_80_85>",
    "<plddt_85_90>",
    "<plddt_90_95>",
    "<plddt_95_100>",
]

# Standard 20 amino acids (3-letter codes)
AMINO_ACIDS = [
    "ALA",
    "ARG",
    "ASN",
    "ASP",
    "CYS",
    "GLN",
    "GLU",
    "GLY",
    "HIS",
    "ILE",
    "LEU",
    "LYS",
    "MET",
    "PHE",
    "PRO",
    "SER",
    "THR",
    "TRP",
    "TYR",
    "VAL",
]

# Atom names (backbone + side chain)
ATOM_NAMES = [
    "C",
    "CA",
    "CB",
    "CD",
    "CD1",
    "CD2",
    "CE",
    "CE1",
    "CE2",
    "CE3",
    "CG",
    "CG1",
    "CG2",
    "CH2",
    "CZ",
    "CZ2",
    "CZ3",
    "N",
    "ND1",
    "ND2",
    "NE",
    "NE1",
    "NE2",
    "NH1",
    "NH2",
    "NZ",
    "O",
    "OD1",
    "OD2",
    "OE1",
    "OE2",
    "OG",
    "OG1",
    "OH",
    "SD",
    "SG",
    "OXT",
]

# Non-canonical residue token
EXTRA_TOKENS = ["<UNK>"]

# Maximum residue position index
MAX_POSITION = 2700

# Backbone atoms valid for all amino acids
_BACKBONE = {"N", "CA", "C", "O", "OXT"}

# Valid heavy atoms per amino acid (backbone + side chain)
VALID_ATOMS: dict[str, set[str]] = {
    "ALA": _BACKBONE | {"CB"},
    "ARG": _BACKBONE | {"CB", "CG", "CD", "NE", "CZ", "NH1", "NH2"},
    "ASN": _BACKBONE | {"CB", "CG", "OD1", "ND2"},
    "ASP": _BACKBONE | {"CB", "CG", "OD1", "OD2"},
    "CYS": _BACKBONE | {"CB", "SG"},
    "GLN": _BACKBONE | {"CB", "CG", "CD", "OE1", "NE2"},
    "GLU": _BACKBONE | {"CB", "CG", "CD", "OE1", "OE2"},
    "GLY": _BACKBONE,
    "HIS": _BACKBONE | {"CB", "CG", "ND1", "CD2", "CE1", "NE2"},
    "ILE": _BACKBONE | {"CB", "CG1", "CG2", "CD1"},
    "LEU": _BACKBONE | {"CB", "CG", "CD1", "CD2"},
    "LYS": _BACKBONE | {"CB", "CG", "CD", "CE", "NZ"},
    "MET": _BACKBONE | {"CB", "CG", "SD", "CE"},
    "PHE": _BACKBONE | {"CB", "CG", "CD1", "CD2", "CE1", "CE2", "CZ"},
    "PRO": _BACKBONE | {"CB", "CG", "CD"},
    "SER": _BACKBONE | {"CB", "OG"},
    "THR": _BACKBONE | {"CB", "OG1", "CG2"},
    "TRP": _BACKBONE | {"CB", "CG", "CD1", "CD2", "NE1", "CE2", "CE3", "CZ2", "CZ3", "CH2"},
    "TYR": _BACKBONE | {"CB", "CG", "CD1", "CD2", "CE1", "CE2", "CZ", "OH"},
    "VAL": _BACKBONE | {"CB", "CG1", "CG2"},
}


def get_all_tokens() -> list[str]:
    """Return all domain-specific vocabulary tokens.

    Does not include utility tokens (pad, eos, newline) which are added
    by the tokenizer setup.
    """
    tokens: list[str] = []
    tokens += CONTROL_TOKENS
    tokens += CORRECTION_TOKENS
    tokens += DISTANCE_BIN_TOKENS
    tokens += PLDDT_TOKENS
    tokens += EXTRA_TOKENS
    tokens += [f"<{aa}>" for aa in AMINO_ACIDS]
    tokens += [f"<{atom}>" for atom in ATOM_NAMES]
    tokens += [f"<p{i}>" for i in range(MAX_POSITION + 1)]
    return tokens


def load_parquet_dataset(
    data_dir: str,
    split: str,
):
    """Load protein docs from local parquet files.

    Args:
        data_dir: Root directory containing train/val/test subdirectories.
        split: One of "train", "val", "test".

    Returns:
        HuggingFace Dataset object with a "document" column.
    """
    import os

    from datasets import load_dataset

    split_dir = os.path.join(data_dir, split)
    data_files = os.path.join(split_dir, "*.parquet")
    ds = load_dataset("parquet", data_files=data_files, split="train")
    return ds


def filter_by_cluster_limit(dataset, max_docs_per_cluster: int, seed: int = 42):
    """Limit the number of documents per struct_cluster_id.

    Randomly selects up to *max_docs_per_cluster* documents from each
    cluster, ensuring no cluster is over-represented during training.

    Args:
        dataset: HuggingFace Dataset with a ``struct_cluster_id`` column.
        max_docs_per_cluster: Maximum documents to keep per cluster.
        seed: Random seed for reproducible subsampling.

    Returns:
        Filtered HuggingFace Dataset.
    """
    rng = np.random.RandomState(seed)

    cluster_to_indices: dict[str, list[int]] = defaultdict(list)
    cluster_ids = dataset["struct_cluster_id"]
    for i, cid in enumerate(cluster_ids):
        cluster_to_indices[cid].append(i)

    keep_indices: list[int] = []
    for indices in cluster_to_indices.values():
        if len(indices) <= max_docs_per_cluster:
            keep_indices.extend(indices)
        else:
            keep_indices.extend(
                rng.choice(indices, size=max_docs_per_cluster, replace=False).tolist()
            )

    keep_indices.sort()
    return dataset.select(keep_indices)
